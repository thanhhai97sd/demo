<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
   
    <title>Promise</title>
    
    <script type="text/javascript">
        let add=(a,b)=>{
            return new Promise((resolve, reject)=>{
                setTimeout(()=>{
                    if(typeof a!='number'||typeof b!='number'){
                        return reject(new Error('Tham số phải là kiểu number'));
                    }
                    resolve(a+b);
                },2000);
            });
        }
        // let a=add(4,5)
        // .then(result=>console.log(result),
        // err=>console.log(err+""));
        // console.log(a);
        // setTimeout(()=>console.log(a), 2500);
        
        // let aPromise=Promise.resolve(add(4,5));
        // aPromise.then(res=>console.log(res));

        let rejPromise= Promise.reject('Bi loi');
        rejPromise.catch(e=>console.log(e+""));
        console.log(rejPromise);
        
        //console.log(aPromise);
        
    </script>
</head>
<body>
    hello, demo
</body>
</html>